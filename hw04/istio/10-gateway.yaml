apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: otus-hw04-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http-otus-hw04
        protocol: HTTP
      hosts:
        - "*"